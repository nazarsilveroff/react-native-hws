
import {StyleSheet} from "react-native";

const CreatePostsScreenStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: "center",
    }
})

export default CreatePostsScreenStyles;
